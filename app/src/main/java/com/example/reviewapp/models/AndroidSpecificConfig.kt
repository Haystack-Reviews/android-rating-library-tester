package com.example.reviewapp.models

import android.content.Context
import androidx.fragment.app.FragmentActivity

data class AndroidSpecificConfig(val applicationContext: Context, val fragmentContext: Context, val activity: FragmentActivity)